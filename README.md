# webt-log

> log($text, $type);

###### Типы логов:
Ошибка = 'error'
Предупреждение = 'warning'
Успех = 'success'
Обычный вывод = 'default'

###### Пример использования:
```javascript
log('Текст', 'success')
